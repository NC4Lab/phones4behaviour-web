export interface FileData {
    fileName: string;
    filePath: string;
    fileType: string;
    uploadTime: string;
}

export interface LogData {
    tag: string;
    desc: string;
    time: string;
}